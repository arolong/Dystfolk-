# Safe migration flag: keep false until backend is verified.
VITE_BACKEND_ENABLED=false

# Frontend endpoint for backend config.
VITE_BACKEND_CONFIG_URL=/api/config

# Netlify function env vars (set in Netlify UI for production):
# PREVENTA_PRICE=15000
# PREVENTA_STOCK=25  # Ajusta entre 20 y 25 segun tu preventa real
# PREVENTA_ACTIVE=true
# GENERAL_PRICE=20000
# GENERAL_STOCK=300
# GENERAL_ACTIVE=true
